const getVibe = function (uuid) {
	//takes a uuid for a bar and returns the vibe as aggregated across reviews
	//TODO implement this
	return 'Dive Bar';
};

const getRating = function (uuid) {
	//takes a uuid for a bar and returns the rating as aggregated across reviews
	//TODO implement this
	return 4.5;
};

export { getVibe, getRating };
